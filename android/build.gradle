group 'com.magugi.volume_watcher'
version '1.0'

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.0'
    }
}

rootProject.allprojects {
    repositories {
        google()
        jcenter()
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 33

    // Specify the namespace for the module. This is required by the new AGP.
    namespace "com.magugi.volume_watcher"
    
    defaultConfig {
        minSdkVersion 16
    }
}